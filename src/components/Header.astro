---
import { navigation } from '../data/navigation';
---
<header class="site-header">
  <div class="shell header-inner">
    {/* TEMPORARY PLACEHOLDER — awaiting canonical LOGO-TAI-003 from
        01 — TechSafe Brand Identity & Design System (asset not yet delivered
        to this workspace; dependency recorded in claude/73). Founder directive
        31 Aug 2026: do not use the old TSI lockup; do not recreate the logo. */}
    <a class="brand" href="/" aria-label="TechSafeAI home">
      <span class="wordmark wordmark-temp">TechSafeAI</span>
    </a>
    <nav class="desktop-nav" aria-label="Primary navigation">
      {navigation.map((item) => <a href={item.href}>{item.label}</a>)}
      <a class="button button-small button-primary" href="/start-with-your-challenge">Start with your challenge</a>
    </nav>
    <details class="mobile-nav">
      <summary aria-label="Open navigation">Menu</summary>
      <nav aria-label="Mobile navigation">
        {navigation.map((item) => <a href={item.href}>{item.label}</a>)}
        <a class="button button-primary" href="/start-with-your-challenge">Start with your challenge</a>
      </nav>
    </details>
  </div>
</header>
